# Detox

Run the following to install Detox:

```
sudo zypper addrepo https://download.opensuse.org/repositories/utilities/openSUSE_Factory/utilities.repo
sudo zypper -n install detox
```

Move `.detoxrc` and `mysafe.tbl` provided in `conf/detox` to your home directory.
