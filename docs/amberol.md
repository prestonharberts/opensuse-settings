# Amberol

Run the following to install Amberol:

```
flatpak --user install flathub io.bassi.Amberol -y
```

Set it as the default when opening MP3 and FLAC files.

Change the following settings:

- Background Playback: Off
